#include <stdio.h>
#include <locale.h>
int main() {
setlocale(LC_ALL, "Portuguese");
float salarioBase, salarioReceber;
printf("Digite o salário base do funcionário: ");
scanf("%f", &salarioBase);
salarioReceber = salarioBase * 1.05; // mais 5%
printf("O salário a receber é: %.2f\n", salarioReceber);
return 0;
}
